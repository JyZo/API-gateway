server:
  port: 9002

spring:
  application:
    name: multiplation
  profiles:
    active: multiplation

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka  # default address

